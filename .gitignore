# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# but keep the template
!.env.example

# sandbox tool output dumps (may contain file previews incl. secrets) — never commit
/tool-results/

# throwaway clones / scratch
/tmp/

# local verification screenshots
scripts/*.png

# Tester sticker kit — printed-QR tokens stay out of the public repo.
# Keep these files locally next to the printed kit; deleting them invalidates the printed stickers.
scripts/tester-hall-manifest.json
download/tester-qr/
download/SeatServe-Tester-QR-Stickers.html
download/SeatServe-Tester-QR-Stickers.pdf
prisma/pg-client/
download/Tester-QR-Stickers.pdf
public/downloads/
upload/
git token
.env.prod-db
scripts/.cookies-ramesh.txt
