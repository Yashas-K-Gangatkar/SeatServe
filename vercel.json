{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/make-postgres-schema.mjs && bunx prisma generate --schema prisma/schema.postgres.prisma && bunx prisma db push --schema prisma/schema.postgres.prisma --skip-generate && next build",
  "installCommand": "bun install",
  "crons": [
    {
      "path": "/api/health?cron=1",
      "schedule": "0 4 * * *"
    }
  ]
}
